devtools::install_github("SANBI-NBA/nbaR", force = T)

# Due to the many updates to the package, it may be necessary to 
# periodically re-render the entire website to ensure package changes
# pull through - but do this with care, as it might 
# also cause other bits of code to break. Remind contributors that it is their
# own responsibility to trigger chunk updates with code edits if they want changes
# in the package to reflect in their content.